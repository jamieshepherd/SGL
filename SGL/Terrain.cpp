/*
--------------------------------------------------------------------
Terrain.cpp
--------------------------------------------------------------------
A class that loads a heightmap, loads terrain textures, builds
vertex arrays and renders terrain once (as terrain does not move).
--------------------------------------------------------------------
*/

#include "Camera.h"
